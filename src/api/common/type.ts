export type code_type = {
  email: string;
};

export type front_commits_res = {
  id: string;
  name: string;
  email: string;
  date: string;
  avatar: string;
  color: string;
  message: string;
}[];
